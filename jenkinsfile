pipeline {
    agent any

    stages {
        stage('ecomm') {
            steps {
                sh 'sudo apt update && sudo apt install nginx -y'
                sh 'sudo cp index.html /var/www/html' 
            }
        }
    }
}
